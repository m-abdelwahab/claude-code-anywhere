# ============================================================
# Agents Anywhere — Environment Variables
# ============================================================
# Copy to .env and fill in values, or set directly in the Railway when deploying the template.

# --- SSH Authentication (set at least one) -------------------

# Your SSH public key — recommended for computers.
# Get it with: cat ~/.ssh/id_ed25519.pub
# Format: ssh-ed25519 AAAA... user@host
SSH_PUBLIC_KEY=

# Password for SSH access — simpler, great for mobile.
# Use a strong password — the SSH port is exposed to the internet.
SSH_PASSWORD=

# --- Platform Tokens (optional) ------------------------------

# GitHub personal access token — lets agents clone repos, create PRs.
# Recommended: Fine-grained PAT scoped to specific repos.
# Get one at: https://github.com/settings/tokens?type=beta
# Format: github_pat_... (fine-grained) or ghp_... (classic)
GITHUB_TOKEN=

# Railway API token — lets agents manage Railway services.
# Get one at: https://railway.com/account/tokens
# Tip: Use a project-scoped token when possible for tighter security.
RAILWAY_API_TOKEN=
